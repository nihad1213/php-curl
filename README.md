# PHP cURL Library

A lightweight PHP library to make HTTP requests easier using cURL. Supports GET, POST, PUT, PATCH, and DELETE methods with custom headers and body. Includes a simple `Client`, `Request`, and `Response` structure for clean and readable code.